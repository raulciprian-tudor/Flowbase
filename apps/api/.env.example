# Environment variables are loaded from the workspace root .env file
# (both Prisma CLI and the API run from the repo root via Nx).
# See ../../.env.example for the full list, including DATABASE_URL.
